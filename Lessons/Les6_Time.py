5. Представьте, что у вас есть три переменные: x=0.1, y=0.2, z=0.3.
Напишите программу, которая складывает эти три числа и печатает результат.
Допустим, что эти три слагаемых представляют количество центов в евро, т.е. 0.1 -
это 10 евроцентов, 0.2 - это 20 евро центов.
Посмотрите на сумму слагаемых. Не смущает ли вас результат? Каким типом
данных можно представить деньги в Python, чтобы избежать наблюдаемую
проблему?

from decimal import Decimal
# Задаем значения переменных с использованием Decimal
x = Decimal('0.1')
y = Decimal('0.2')
z = Decimal('0.3')

# Складываем числа
total = x + y + z

# Печатаем результат
print(f"Сумма: {total}")

-------
6. Сейчас на часах 9 часов 45 минут утра.
Напишите программу, которая считает количество секунд, прошедшее с полуночи.

Задаем часы и минуты
hours = 9
minutes = 45
# Вычисляем количество секунд
total_seconds = (hours * 3600) + (minutes * 60)
# Выводим результат
print(f"Количество секунд, прошедших с полуночи: {total_seconds} секунд")

-------
7. Напишите программу, которая считает количество секунд если любое другое время в течение дня?
# Запрашиваем у пользователя часы и минуты
hours = int(input("Введите часы (0-23): "))
minutes = int(input("Введите минуты (0-59): "))

# Проверяем допустимость ввода
if (0 <= hours < 24) and (0 <= minutes < 60):
    # Вычисляем количество секунд
    total_seconds = (hours * 3600) + (minutes * 60)

    # Выводим результат
    print(f"Количество секунд, прошедших с полуночи: {total_seconds} секунд")
else:
    print("Ошибка: неверный ввод времени. Проверьте часы и минуты.")

-------
10. Дан прямоугольный треугольник с катетами 3 и 4 метра. Напишите программу,
которая считает длину гипотенузы.

import math

# Задаем длины катетов
a = 3  # Первый катет
b = 4  # Второй катет

# Вычисляем длину гипотенузы по теореме Пифагора
c = math.sqrt(a**2 + b**2)

# Выводим результат
print(f"Длина гипотенузы: {c} метров")

------
11. Посчитайте в уме результат выражения 10+3*(7-3)/2. Напишите программу, которая
#вычислит значение выражение. Проверьте себя.

# Вычисляем значение выражения
result = 10 + 3 * (7 - 3) / 2

# Выводим результат
print(f"Результат выражения: {result}")